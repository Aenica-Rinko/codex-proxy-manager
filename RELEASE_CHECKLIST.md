# 预发布与 v1.0 验收清单

## v0.10 已实现的发布准备

- 单一版本源：`manager/AppInfo.cs`，同步窗口、诊断报告、EXE 文件名与程序集版本。
- 首次引导、只读安装检查、缺文件时阻止继续；v0.10.2 起所有用户均须手动点击启动应用，旧自动启动设置不再生效。
- 管理器专用预发布 ZIP、逐文件 SHA-256 清单和 ZIP 外部校验文件。
- GPL-3.0-only 完整许可证、Rinko 署名、随包对应源码和构建脚本；使用包内暂存源码构建 EXE，并测试解压后独立重新编译。
- 明确文件名单打包，排除私人配置、缓存、日志、快捷方式、旧 EXE、核心和规则库。
- 不依赖已安装 Codex/核心的单元测试，以及本地完整网络回归入口。
- 打包前/暂存后凭据模式检查、Git 工作区候选文件审计、中文路径重编译回归；本地覆盖 PowerShell 7 和 Windows PowerShell 5.1。v0.10.1 已通过 GitHub 云端验证；每个后续提交仍需检查自己的 CI 结果。

## 本地验证命令

在源码根目录执行：

```powershell
& '.\manager\build.ps1'
& '.\manager\test-release-safety.ps1'
& '.\manager\audit-release.ps1'
& '.\manager\test.ps1' -UnitOnly
& '.\manager\test-package.ps1'
& '.\manager\test.ps1' # 需要本地 Codex、核心和规则库
& '.\manager\package.ps1'
```

构建无需 Visual Studio，使用系统 .NET Framework 编译器。打包默认写入 `dist`，已有同版本 ZIP 时拒绝覆盖，可通过 `-OutputDirectory` 选择另一个目录。工作流仅做构建、单元测试和打包检查，不自动创建 GitHub Release、不下载核心、不使用私人节点。

打包名单不代替内容审计：发布前仍须检查允许打包的文档、源码和模板本身是否被误写入凭据。哈希用于发现字节变化，不是签名或来源保证。

自动审计仅覆盖当前工作区中的 Git 发布候选文件，不覆盖暂存区旧内容、历史、未引用对象，也不能发现所有编码或未知格式凭据。输入检查报告不回显密钥；不会访问真实用户配置或外部代理服务。已加入防泄漏测试不代表人工审计门槛自动完成。

## v1.0 稳定版之前必须完成

- [x] 所有者确认作者署名为 Rinko（2026-09-06）。
- [x] 所有者选定 GNU GPL 第 3 版；本项目采用 GPL-3.0-only，完整许可证见 LICENSE（2026-09-06）。
- [ ] 完成拟发布资产的第三方许可审计；项目授权不替换第三方许可。
- [x] 对 v0.10.2 功能提交及此前可遍历历史完成文件边界、提交身份、常见凭据模式和已知私人标记复核（2026-09-09）；范围与局限见 ACCEPTANCE.md。每次新增材料仍须复核，不能保证不存在未知秘密。
- [ ] 如发布完整运行包，记录核心和规则库的确切来源、版本、哈希，补齐各组件许可证及所需源码材料；当前脚本不支持捆绑它们。
- [ ] 在干净 Windows 10/11 x64 中记录构建号、运行库条件与结果；测试中文路径、标准用户权限、125%/150% DPI。
- [ ] 在新用户目录完成导入、启动、节点切换、停止、异常恢复及升级保留配置测试。
- [ ] 验证 Microsoft Store、游戏和其他独立软件的原网络路径；这需要实际应用验收，不能用单元测试代替。
- [ ] 覆盖四类来源；外部接口鉴权失败、上游未启动、端口占用、无效订阅和核心不兼容应有可理解的错误。
- [ ] 明确发布者身份、签名策略、支持范围、已知问题及反馈渠道。
- [x] 在真正的 GitHub 私有仓库运行构建工作流并审阅 v0.10.1、v0.10.2 功能提交的通过结果；后续提交仍需各自通过 CI，详见 ACCEPTANCE.md。

只有验收达到标准才标记 v1.0；需要时先发布 v0.11 修复，不按版本号强行跳过检查。

## 当前验收状态

2026-09-09 所有者决定先公开 v0.10.2 管理器预览版，性能问题留待后续更新。README 与发布说明明确标注：已收到启动应用后 Microsoft Store/游戏联网变慢的反馈，原因未定位、未修复。不将上述未完成项标记为通过，不发布为 v1.0。发布状态与附件以 GitHub Releases 为准；旧候选包和旧 v0.10.1 包不作为本次附件。

## 仍需人工完成的用户流程（不要用单元测试代填）

以下检查应在独立的 Windows 标准用户或测试机完成。记录版本、步骤和通过/失败即可，测试记录不要包含真实凭据或个人路径。

| 步骤 | 预期结果 | 人工结果 |
| --- | --- | --- |
| 从候选 ZIP 完整解压，验证 SHA-256 | 校验相符；有 EXE、完整许可证、源码和说明，无私人设置 | 待验证 |
| 未准备核心/规则时首次打开 | 提示缺失文件，不能继续；不启动目标应用 | 待验证 |
| 从可信上游补齐文件，重新检查 | 可以继续配置，不要求关闭系统安全保护 | 待验证 |
| 分别导入四类来源，保存、重新打开 | 档案/节点可见；不自动启动核心或应用 | 待验证 |
| 旧配置含自动启动，升级后打开 | 忽略旧自动启动，应用、档案和收藏保留 | 待验证 |
| 主动启动 Codex 或另一兼容应用 | 仅所选应用会话启动，实际服务请求成功 | 待验证 |
| 手动切换节点、停止并重新启动 | 状态正确；能理解现有连接会中断及应用需重启的提示 | 待验证 |
| 关闭面板、从托盘退出 | 关闭仅最小化；退出按预期停止核心 | 待验证 |
| Microsoft Store、游戏及独立浏览器共存 | 原网络可用；记录其他客户端的 TUN/系统代理影响 | 已收到启动后变慢的反馈，未通过，待定位与修复 |
| 中文路径、标准用户、125%/150% DPI | 无截断、遮挡、权限错误；控件可操作 | 待验证 |

本次公开范围限于 Rinko 署名的源码、文档以及新构建的 manager-only-preview ZIP 和校验文件，不包含运行库、私人配置备份、旧包或本机截图。完整运行包和 v1.0 仍需单独完成相应验收。
