# 预发布与 v1.0 验收清单

## v0.10 已实现的发布准备

- 单一版本源：`manager/AppInfo.cs`，同步窗口、诊断报告、EXE 文件名与程序集版本。
- 首次引导、只读安装检查、缺文件时阻止继续；v0.10.2 起所有用户均须手动点击启动应用，旧自动启动设置不再生效。
- 管理器专用预发布 ZIP、逐文件 SHA-256 清单和 ZIP 外部校验文件。
- GPL-3.0-only 完整许可证、Rinko 署名、随包对应源码和构建脚本；使用包内暂存源码构建 EXE，并测试解压后独立重新编译。
- 明确文件名单打包，排除私人配置、缓存、日志、快捷方式、旧 EXE、核心和规则库。
- 不依赖已安装 Codex/核心的单元测试，以及本地完整网络回归入口。
- 打包前/暂存后凭据模式检查、Git 工作区候选文件审计、中文路径重编译回归；本地覆盖 PowerShell 7 和 Windows PowerShell 5.1，CI 中也已配置但尚未云端验证。

## 本地验证命令

在源码根目录执行：

```powershell
& '.\manager\build.ps1'
& '.\manager\test-release-safety.ps1'
& '.\manager\audit-release.ps1'
& '.\manager\test.ps1' -UnitOnly
& '.\manager\test-package.ps1'
& '.\manager\test.ps1' # 需要本地 Codex、核心和规则库
& '.\manager\package.ps1'
```

构建无需 Visual Studio，使用系统 .NET Framework 编译器。打包默认写入 `dist`，已有同版本 ZIP 时拒绝覆盖，可通过 `-OutputDirectory` 选择另一个目录。工作流仅做构建、单元测试和打包检查，不自动创建 GitHub Release、不下载核心、不使用私人节点。

打包名单不代替内容审计：发布前仍须检查允许打包的文档、源码和模板本身是否被误写入凭据。哈希用于发现字节变化，不是签名或来源保证。

自动审计仅覆盖当前工作区中的 Git 发布候选文件，不覆盖暂存区旧内容、历史、未引用对象，也不能发现所有编码或未知格式凭据。输入检查报告不回显密钥；不会访问真实用户配置或外部代理服务。已加入防泄漏测试不代表人工审计门槛自动完成。

## 正式开源/1.0 之前必须完成

- [x] 所有者确认作者署名为 Rinko（2026-09-06）。
- [x] 所有者选定 GNU GPL 第 3 版；本项目采用 GPL-3.0-only，完整许可证见 LICENSE（2026-09-06）。
- [ ] 完成拟发布资产的第三方许可审计；项目授权不替换第三方许可。
- [ ] 审计待提交源码与 Git 历史，排除订阅、节点凭据、日志、截图中的私人信息。
- [ ] 如发布完整运行包，记录核心和规则库的确切来源、版本、哈希，补齐各组件许可证及所需源码材料；当前脚本不支持捆绑它们。
- [ ] 在干净 Windows 10/11 x64 中记录构建号、运行库条件与结果；测试中文路径、标准用户权限、125%/150% DPI。
- [ ] 在新用户目录完成导入、启动、节点切换、停止、异常恢复及升级保留配置测试。
- [ ] 验证 Microsoft Store、游戏和其他独立软件的原网络路径；这需要实际应用验收，不能用单元测试代替。
- [ ] 覆盖四类来源；外部接口鉴权失败、上游未启动、端口占用、无效订阅和核心不兼容应有可理解的错误。
- [ ] 明确发布者身份、签名策略、支持范围、已知问题及反馈渠道。
- [ ] 在真正的 GitHub 仓库运行构建工作流并审阅结果。目前本地通过不代表云端已验证。

只有验收达到标准才标记 v1.0；需要时先发布 v0.11 修复，不按版本号强行跳过检查。

## 当前验收状态

2026-09-06 开始本地验收，并修复外部接口测试阻塞窗口的问题（v0.10.1）。详细证据、环境和未执行项记录在源码根目录的 `ACCEPTANCE.md`。以上正式发布条件没有因为本机测试通过而自动完成。
