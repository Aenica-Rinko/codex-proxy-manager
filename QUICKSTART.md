# 从零开始使用 Codex Proxy Manager

这是发布准备版本，不是已完成独立 Windows 验收的 v1.0。新生成的 `manager-only-preview.zip` 含管理器、说明、恢复工具、LICENSE，以及 `manager/` 对应源码和构建脚本；不含代理核心、规则库、节点或订阅，不是开箱即用的完整安装包。项目采用 GPL-3.0-only，署名 Rinko，完整条款见 LICENSE；当前尚未公开发布。旧包未自动更新，公开发布前须重新构建和打包。

## 1. 获取并解压管理器

当前仓库为预发布源码项目。GitHub 的“Download ZIP”下载的是源码，不是带 EXE 的安装包；没有 Release 时，不要去陌生网站搜索同名安装器。

取得源码后，完整解压到普通、可写且不公开同步的文件夹，在该文件夹打开 PowerShell，运行：

```powershell
& '.\manager\build.ps1'
```

成功后，文件夹根目录会生成 `CodexProxyManager.v0.10.2.exe`。该步骤只编译管理器，不启动代理，也不需要节点或已安装 Codex。不需要 Visual Studio；构建脚本使用 Windows .NET Framework 编译器。

如果你拿到的是维护者生成的 `manager-only-preview.zip`，它已经有 EXE，可以跳过编译；仍须完整解压，不要在 ZIP 内直接运行。当前没有自动安装器或自动更新功能。遇到脚本策略限制应遵循本机管理策略，不建议为了本项目全局关闭安全限制。

## 2. 准备代理核心和规则文件

目标是 Windows x64，程序使用 .NET Framework 4.x。本地已测试环境不等于所有 Windows 版本均受支持；干净 Windows 10/11 与高 DPI 验收仍在发布清单中。

从可信来源取得与你的系统兼容的 Mihomo Windows amd64 核心，解压并命名为 `mihomo.exe`。请从[上游安装说明](https://wiki.metacubex.one/startup/)进入下载渠道，不使用陌生人的替换 EXE。当前本地回归使用的核心自报版本为 v1.19.24；这不是对该本地文件来源的认证，也不保证所有核心版本兼容。

规则文件可从 [MetaCubeX/meta-rules-dat](https://github.com/MetaCubeX/meta-rules-dat) 取得 `geosite.dat` 和 `geoip.metadb`。保存成下列目录结构（Windows 通常不区分文件名大小写）。不要将不同格式的数据库仅改名冒充另一种格式。

```text
解压目录/
├── CodexProxyManager.v0.10.2.exe
├── LICENSE
├── QUICKSTART.md
├── manager/                 # 随源码/新预发布包提供的构建源码
└── runtime/
    ├── mihomo.exe
    └── data/
        ├── GeoSite.dat
        └── geoip.metadb
```

下载时核对上游发布说明、资产名称及可用的校验信息。v0.10 不自动下载、安装或验证第三方运行文件。升级用户可以继续使用已有运行文件，不必重新下载。首次引导也接受用户目录中已有的规则库。

四种代理来源都需要上述文件；“外部客户端”不是免核心模式。实际使用 Codex 时还需要已安装的 Codex Windows 应用及你自己的可用账号；本项目不会代为安装、注册或提供服务访问资格。

## 3. 添加第一个代理档案

首次打开会显示引导和文件检查。缺文件时先补齐，再点“重新检查”；“继续配置”仅在文件存在且非空时可用。文件检查不是兼容性、安全性或连通性认证。

选择适合自己的来源：

| 你已经有的内容 | 档案来源 | 是否还需其他客户端运行 |
| --- | --- | --- |
| 订阅 URL | 订阅 | 不需要 |
| 节点分享链接 | 分享链接 | 不需要 |
| 其他代理软件的 HTTP/SOCKS5 端口 | 本地代理 | 需要 |
| 本机 Clash/Mihomo 控制接口与密钥 | 外部 Clash/Mihomo API | 需要 |

管理器本身不提供节点或订阅。控制接口只接受本机地址；不要把订阅或控制密钥贴进公开 Issue。

在首次配置窗口的“档案名称”填写自己容易辨认的名称，在“节点来源”选择以下一种方式，填写后点击“添加并设为当前”，再点击“完成”进入面板。不要为本教程向仓库里的 `config.example.yaml` 填写真实凭据。

### A. 使用订阅 URL

选择“订阅 URL”，把自己的完整订阅地址粘贴到“订阅地址”。内容可以是兼容的 Mihomo/Clash YAML、逐行分享链接或 Base64。订阅通常包含访问凭据，请只填入管理器，不粘贴到反馈或聊天记录。

来源导入的是节点信息，不是保证完整复制其他客户端的所有路由与 UI 设置。添加档案成功不代表订阅已成功下载；启动后检查节点列表。如果没有节点，再检查订阅是否有效、可访问以及格式是否被当前核心支持。

### B. 使用节点分享链接

选择“分享链接 / Base64”，每行粘贴一个完整分享链接；也支持兼容的 Base64 内容。保留协议前缀及必要参数，不要只复制服务器 IP 或名称。不要使用本教程之外的不明转换网站提交自己的链接。

**仅有服务器 IP 并不足以添加节点。** 还需要已配置的协议、端口及相应认证/TLS 等参数，通常由提供者生成完整分享链接。本项目不会在服务器上创建代理服务。

### C. 使用已有软件的本地代理

先打开上游代理软件，找到它实际监听的 HTTP、SOCKS5 或 mixed 端口。选择“本地 HTTP / SOCKS5”，使“协议”与上游端口类型一致，“地址”通常为 `127.0.0.1`，“端口”以该软件设置为准，不要照抄别人的值。仅在上游启用了代理鉴权时填写用户名和密码。

点击“测试本地端口”检查能否连接，然后添加档案。端口测试成功只证明端口可连接，不证明外网、鉴权或目标服务可用。上游软件必须保持运行；本管理器通常只看到一个本地上游入口，无法仅凭这个端口列出上游的所有节点，要在上游软件内切换。

### D. 控制外部 Clash / Mihomo 节点

先在外部客户端确认以下四项，再选择“外部 Clash / Mihomo API”：

| 字段 | 应填内容 | 注意 |
| --- | --- | --- |
| 流量地址 / 端口 | 外部客户端的本地 HTTP 或 mixed 入口 | 用于应用流量；不是控制 API 端口 |
| 控制接口 | 例如 `http://127.0.0.1:9090`，以实际设置为准 | 只允许本机地址；不要暴露到公网 |
| 控制密钥 | 外部客户端配置的 API secret | 不等于节点密码，不要公开 |
| 策略组 | 希望控制的准确组名 | 留空会自动选节点最多的策略组 |

点击“测试控制接口”检查鉴权和接口结构。成功后仍需保存、启动并实际测试应用。外部客户端必须运行；本管理器切换的是它的策略组，其他使用同一策略组的流量也会受影响。

如果你只想代理目标应用，请自行确认外部客户端的系统代理/TUN 状态是否符合你的需求。本项目不会替你切换这些全局开关，也不需要你把本地控制接口开放给其他设备。

打开管理器或保存设置后只进入面板，不启动应用或代理核心。v0.10.2 已删除自动启动功能，即使旧配置中曾启用也不再生效；只有主动点击启动项才会启动当前应用。默认应用是 Codex；其他兼容应用可从“管理应用”添加。不能通过添加 EXE 透明接管任意游戏或 UWP 程序。

## 4. 启动、验证、切换和停止

完全退出所选应用后，在管理器点击启动。可查看节点、速度、收藏或手动切换，失败时先打开“连接诊断”。诊断通过、节点测速成功都不代表目标服务可以登录。

本项目正常会话不修改 Windows 系统代理。上游客户端自己的系统代理、TUN、VPN 设置不由本项目管理。关闭管理窗口会进入托盘；停止代理后，仍运行的目标应用需要退出并重新启动。

建议首次使用按以下顺序验证：

1. 完全退出目标应用，包括已有后台实例，然后点击“启动应用”。
2. 等待面板显示会话状态和节点；如需择优，点击“测试节点延迟”。
3. 单击目标节点，再点击“切换到选中节点”；或点击“选择最低延迟”。切换会重建连接，可能中断当前请求。
4. 在目标应用中实际发送一次请求，再查看活动连接和上传/下载流量。0 B/s 可能仅表示空闲；这些统计不是全机网速。
5. 使用结束后退出目标应用，等待管理器检测并清理；也可点击“停止专用代理”。若要彻底退出管理器，使用“退出管理器”，不要只点窗口关闭按钮。

应用仅手动启动；自动选优和连续失败切换仍是可选设置，只在手动启动会话后工作。搜索或“仅看收藏”只过滤列表，不改变自动选优范围。详细行为见 [USER_GUIDE.md](USER_GUIDE.md)。

## 5. 更新、数据与卸载

主面板“开始使用 / 版本”随时重开引导；也可运行 `CodexProxyManager.v0.10.2.exe --guide`，它不启动代理或配置向导。配置、节点和收藏位于 `%LOCALAPPDATA%\CodexProxyManager`，不要提交该目录。

ZIP 同目录的 `.sha256` 用来核对 ZIP 字节，ZIP 内 `manifest.json` 记录各文件的 SHA-256。哈希不是数字签名：如果哈希文件也被替换，它不能证明发布者身份。源代码仓库里的 `manager/package.ps1` 使用明确文件名单打包，不扫描或复制用户数据目录。

手动更新前先停止会话并退出管理器，再把新版本完整解压到程序目录或新文件夹。保留自己的用户数据和必要 runtime；不要用示例模板覆盖真实档案，也不要将运行中的 EXE 强行覆盖。若换了 EXE 名称或目录，需要更新自己的快捷方式。

移除程序前先停止代理并退出。程序目录与用户数据目录相互独立：移除程序不等于删除订阅与日志。需要清除个人数据时，先自行备份并确认范围，再处理上述用户数据目录；本指南不提供一键不可恢复删除命令。

## 6. 常见问题

| 现象 | 先检查什么 |
| --- | --- |
| 找不到 EXE | 下载的是否为源码 ZIP；构建是否成功 |
| “继续配置”不可用 | 引导列出的文件是否存在且非空；规则数据库格式是否正确 |
| 找不到 Codex | 当前用户是否已安装 Codex；不同安装方式可能不被自动定位支持 |
| 显示拒绝访问 | 是否在 ZIP/受限目录运行，文件是否被占用或被安全软件隔离；不要先关闭防护或改 WindowsApps 权限 |
| 一直重连 | 旧应用是否完全退出、核心是否启动、当前节点能否访问目标服务；用“连接诊断”区分问题 |
| 只有一个节点 | 是否选择了“本地 HTTP / SOCKS5”；这种方式通常只显示上游入口。也检查搜索和收藏筛选 |
| 外部接口失败 | 区分流量端口与 API 端口，检查 secret、上游运行状态及本机接口地址 |
| 节点延迟低但使用慢 | 延迟不等于带宽；看丢包、节点负载及应用请求表现，不承诺切换后一定更快 |
| 停止后应用不能联网 | 它仍持有旧代理启动参数；完全退出后再按需要重新启动 |
| 商店或游戏仍有网络问题 | 本管理器不切系统代理，但其他客户端可能已开启系统代理/TUN/VPN；不要将其状态误认为由本项目设置 |

需要恢复检查时可参考详细手册中的“异常恢复”；恢复工具不是万能网络重置，也不会修复任意 VPN/TUN 配置。分享反馈前先阅读 [隐私说明](PRIVACY.md)，不要上传原始配置、日志或完整桌面截图。
