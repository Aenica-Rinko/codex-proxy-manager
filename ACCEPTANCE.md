# v1.0 前本地验收记录

日期：2026-09-06。当前验收修复版本：v0.10.1。

结论：本机检查通过，但尚不能宣布 v1.0 或正式开源发布。本文件记录测试范围，不是安全认证或第三方应用全面兼容保证。

## 测试环境

- Windows 11 x64。
- 当前进程为非管理员权限；不是另建的全新 Windows 用户。
- .NET Framework 4.x。
- PowerShell 7、Windows PowerShell 5.1。
- 本机核心自报 Mihomo v1.19.24；未据此认证文件来源。
- 未使用独立干净的 Windows 虚拟环境；没有在本轮安装或启用系统功能。

## 已执行

| 检查 | 结果与范围 |
| --- | --- |
| 默认构建 | 通过，生成 v0.10.1，程序集版本由同一版本源生成 |
| PowerShell 7 完整回归 | 通过：真实隔离核心启停、应用流量到本机测试上游、原档案保留、系统代理设置前后相同、watchdog 清理 |
| Windows PowerShell 5.1 单元测试 | 通过；不等于完整网络回归也在该 shell 下运行 |
| 两种 PowerShell 打包测试 | 通过：明确文件名单、模拟私人文件/运行库排除、ZIP/文件哈希、拒绝覆盖已有包 |
| 新用户数据模拟与升级 | 临时目录测试通过：缺文件检查不写设置、首次自动启动关闭、已有自动启动偏好保留 |
| 外部接口测试修复 | 本机模拟接口通过：正确/错误鉴权、异常结构、拒绝重定向、不继承系统代理、超时、窗口响应性、检查中关闭窗口 |
| 源码有限敏感信息扫描 | 在项目文本源码、脚本、文档和模板中未匹配私钥头、常见 GitHub/OpenAI token 格式，以及本项目已知私人服务器地址/本机用户名路径 |
| Git 状态检查 | 当前所有源码均未提交，没有可遍历的提交历史（0 个提交），没有配置远端；尚未上传 |

敏感信息扫描只是有限模式检查，不证明没有其他形式的凭据。没有读取或打包真实用户配置目录，也没有扫描其他项目。没有对 Git 未引用对象作完整取证审计。新目录测试不等同于干净操作系统测试。

## 本轮修复

档案管理的外部控制接口测试原来使用 UI 线程上的同步 WebClient 请求。现在在后台执行，并对每次连接/读取设置 2.5 秒超时。点击时固定输入，检查期间限制重复点击，窗口关闭后不更新已释放控件。只显示策略组数量或固定错误说明，不显示服务器原始错误。

检查只访问填写的本机控制接口，不保存档案、不切换节点。两次接口请求的超时分别计算；持续慢速发送响应的接口仍可能让后台工作持续更久，不应把单次读超时理解为整个检查的绝对时间上限。

## 尚未完成的发布门槛

- 项目授权已确认（2026-09-06）：GPL-3.0-only，署名 Rinko。已加入完整 LICENSE；第三方资产许可审计仍未完成。旧 EXE/ZIP 未在此次授权变更中替换，公开发布前须从更新后的源码重新构建和打包。
- 独立干净 Windows 10/11 环境、标准新用户、中文安装路径和 125%/150% DPI：未完成。
- 四类来源在真实外部客户端/订阅服务上的端到端测试：未完成。本机模拟不能代替真实客户端验收。
- Microsoft Store、游戏等实际应用共存：本轮没有启动或测试这些应用；系统代理注册项不变只是部分证据。
- GitHub 托管构建：已创建私有远端仓库并验证连接器访问；源码尚未推送，工作流未在云端执行。
- 完整运行包的核心/规则库来源、许可证和对应源码材料：未完成；预发布包仍不含运行库。
- 发布者身份、签名方案、支持范围和反馈渠道：待确定。

### 许可证落实补验（2026-09-06）

- 已加入 GNU 官方 GPL 第 3 版完整文本；项目授权标识为 GPL-3.0-only，版权署名为 Copyright (c) 2026 Rinko。第三方组件保持原许可。
- PowerShell 7 单元测试通过；未重新执行真实代理网络集成测试，本次未改动代理配置或连接逻辑。
- PowerShell 7 / Windows PowerShell 5.1 打包测试通过：完整 LICENSE、作者及授权元数据、对应源码独立重新编译、源码目录额外文件排除、私人数据/核心排除、文件及 ZIP 哈希、同名包拒绝覆盖。
- 仅在临时测试目录生成验证产物；未替换现有 EXE 和 dist 旧包，未上传 GitHub，尚未完成上述独立 Windows 和公开发布门槛。

### 发布安全检查补验（2026-09-06）

- 新增只读工作区候选文件审计，并在打包输入及暂存副本检查常见凭据模式。检查失败时只输出输入序号、行号、规则，不输出匹配内容或文件名；拒绝继续构建发布包。
- PowerShell 7 / Windows PowerShell 5.1：安全检查回归、工作区审计、单元测试和打包回归通过。
- 模拟案例覆盖六类凭据模式、合法示例、目录穿越/ADS、私人配置名、文件缺失/二进制内容、父目录链接、Git 中文文件名和已跟踪但被忽略的私人文件。
- 允许名单内 README 注入合成凭据后，打包拒绝且输出目录没有发布文件；既有包拒绝覆盖；中文及空格路径下解压后的对应源码可重新编译。
- 这不是完整秘密检测，不覆盖暂存区旧内容或 Git 历史，不能排除未知/编码/拆分秘密。本轮未跑真实代理网络集成、未替换在用 EXE 或旧 ZIP、未上传或运行 GitHub CI。

### 首次提交前复核（2026-09-07）

- 40 个源码、文档和工作流候选文件通过工作区安全检查；未将个人配置、日志、运行库或旧发布包纳入候选范围。
- PowerShell 7 安全检查回归、离线单元测试和打包回归再次通过；本轮未执行真实代理网络集成测试。
- 私有仓库访问已验证，但连接器写入接口不支持显式指定提交作者邮箱；远端首次提交需使用已核实的隐私署名方式。未因获得仓库权限而自动公开项目。

### v0.10.2 手动启动小更新（2026-09-09）

- 删除管理器首次定时刷新中的自动启动入口、档案界面的自动启动勾选项，以及设置到运行时的 AutoStart 传递。启动入口仅保留面板按钮和托盘菜单的主动点击。
- 旧配置中的 `AutoStart: true` 在读取时忽略，不为升级而改写真实配置；下次正常保存时去掉该废弃字段。自动选优、故障切换、节点及应用设置不受此变更影响。
- PowerShell 7 / Windows PowerShell 5.1 离线单元测试、打包回归通过。新增覆盖旧字段兼容、读取不改写、保存移除废弃字段、其他偏好保留、自动启动控件删除、手动点击事件仍可触发。
- 工作区发布候选审计和源码构建通过，生成 v0.10.2 EXE；项目启动快捷方式改为指向新版。未重启在用管理器或目标应用，未改动正在运行的代理规则或系统代理。
- 前一版 v0.10.1 首次提交已通过 GitHub 云端构建和测试；本次 v0.10.2 尚未推送或运行云端 CI，未执行真实代理端到端测试或独立 Windows 验收。

## 复现入口

```powershell
& '.\manager\build.ps1'
& '.\manager\test-release-safety.ps1'
& '.\manager\audit-release.ps1'
& '.\manager\test.ps1' -UnitOnly
& '.\manager\test.ps1'
& '.\manager\test-package.ps1'
```

完整回归依赖本机已安装 Codex 和项目 runtime；仅单元测试/打包检查不依赖这些资产。下一阶段先补足上述验收和所有者决定，再考虑 v1.0；不要只修改版本号。
